version: 2.1

orbs:
  win: circleci/windows@5.0.0

jobs:
  test:
    executor:
      name: win/default
    steps:
    - checkout
    - run: "go version"
    - run: "go build"

workflows:
  main:
    jobs:
    - test